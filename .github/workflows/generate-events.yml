name: Génération automatique de events.js

on:
  workflow_dispatch:  # Permet de lancer le workflow manuellement depuis GitHub

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v3

      - name: 🐍 Installer Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Installer les dépendances
        run: |
          python -m pip install --upgrade pip
          pip install ics pytz requests

      - name: ⚙️ Exécuter le script Python
        run: python generate_events.py

      - name: 🔐 Configurer le dépôt avec le token GitHub
        run: |
          git remote set-url origin https://x-access-token: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@github.com/dioufousmane/calendriermosae.git


      - name: ✅ Commit et push si events.js a changé
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add events.js
          git diff --quiet && git diff --staged --quiet || git commit -m "🔄 Mise à jour automatique de events.js"
          git push
