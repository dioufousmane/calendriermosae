name: Générer les événements ESGT & UNIV

on:
  workflow_dispatch:  # Lancement manuel
  schedule:
    - cron: "0 4 * * 1"  # Tous les lundis à 4h du matin

jobs:
  generate-events:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v3

      - name: 🐍 Installer Python et dépendances
        run: |
          python -m pip install --upgrade pip
          pip install ics pytz requests

      - name: ⚙️ Exécuter les scripts Python
        run: |
          python generate_esgt_json.py
          python generate_univ_json.py

      - name: 📤 Commit et push des fichiers JSON
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add esgt_events.json univ_events.json

          # S'il n'y a aucun changement, on évite le commit
          if git diff --cached --quiet; then
            echo "✅ Aucun changement à committer."
          else
            git commit -m "🔄 Mise à jour automatique des événements JSON"
            git push
          fi
