name: Générer et déployer les événements

on:
  workflow_dispatch:   # déclenchement manuel
  schedule:
    - cron: "0 4 * * 1" # tous les lundis à 4h du matin

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v3

      - name: 🐍 Installer Python et dépendances
        run: |
          python -m pip install --upgrade pip
          pip install ics pytz requests

      - name: ⚙️ Générer ESGT + UNIV
        run: |
          python generate_esgt_json.py
          python generate_univ_json.py

      - name: 📤 Commit et push des JSON
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add esgt_events.json univ_events.json
          git commit -m "🔄 Mise à jour auto des fichiers JSON"
          git push

  # (optionnel) Tu peux ajouter un job de déploiement si besoin
