name: Mise à jour du calendrier ESGT

on:
  workflow_dispatch:  # Permet de lancer manuellement le workflow

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🔁 Cloner le dépôt
        uses: actions/checkout@v3

      - name: 🐍 Installer Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Installer les dépendances Python
        run: |
          python -m pip install --upgrade pip
          pip install ics pytz requests

      - name: ⚙️ Générer le fichier events.js
        run: python generate_events.py

      - name: ✅ Commit et push si modifications
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          git add events.js
          git diff --quiet && git diff --staged --quiet || git commit -m "🔄 Maj automatique des événements ESGT"
          git push
