name: Générer événements UNIV

on:
  workflow_dispatch:
  schedule:
    - cron: "0 5 * * 1"  # chaque lundi à 5h du matin

jobs:
  generate-univ-events:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v3

      - name: 🐍 Installer Python et dépendances
        run: |
          python -m pip install --upgrade pip
          pip install ics pytz requests

      - name: ⚙️ Générer le fichier JSON UNIV
        run: |
          python esgt_generate_events.py

      - name: 📤 Commit et push du fichier
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add univ_events.json
          git commit -m "🔄 Mise à jour automatique des événements UNIV"
          git push
