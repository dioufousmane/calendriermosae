name: 🔄 Générer les événements UNIV

on:
  workflow_dispatch:  # Lancement manuel
  schedule:
    - cron: "30 4 * * 1"  # Chaque lundi à 4h30 (décalage pour éviter collision)

jobs:
  univ:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v3

      - name: 🐍 Installer Python et dépendances
        run: |
          python -m pip install --upgrade pip
          pip install ics pytz requests

      - name: ⚙️ Exécuter le script UNIV
        run: |
          python univ_generate_events.py

      - name: 📤 Commit et push du fichier UNIV
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add static/univ_events.json

          if git diff --cached --quiet; then
            echo "✅ Aucun changement à committer pour UNIV."
          else
            git commit -m "🔄 Mise à jour UNIV automatique"
            git push
          fi
