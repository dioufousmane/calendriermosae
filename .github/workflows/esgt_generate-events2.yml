name: Générer les événements M2 ESGT 

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */2 * * *"  # Toutes les 2 heures (UTC)

jobs:
  generate-esgt:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v3

      - name: 🐍 Configurer Python 3.9
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"

      - name: 🧪 Vérifier la version de Python
        run: python --version

      - name: 📦 Installer les dépendances
        run: |
          python -m pip install --upgrade pip
          pip install ics pytz requests

      - name: ⚙️ Générer le fichier JSON ESGT
        run: |
          python esgt_generate_events2.py

      - name: 📤 Commit & push si le fichier a changé
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"

          if [ -f esgt_events.json ]; then
            git add esgt_events.json

            if git diff --cached --quiet; then
              echo "✅ Aucun changement détecté."
            else
              git commit -m "🔄 Mise à jour automatique ESGT"
              git push
            fi
          else
            echo "❌ Fichier esgt_events.json non généré."
            exit 1
          fi
