name: Générer les événements ESGT

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */2 * * *"  # Toutes les 2 heures

jobs:
  generate-esgt:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v3

      - name: 🐍 Installer Python et les dépendances
        run: |
          python -m pip install --upgrade pip
          pip install ics pytz requests

      - name: ⚙️ Générer le fichier JSON ESGT
        run: |
          python esgt_generate_events.py

      - name: 📤 Commit & push si changement
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add esgt_events.json

          if git diff --cached --quiet; then
            echo "✅ Aucun changement à committer pour ESGT."
          else
            git commit -m "🔄 Mise à jour ESGT automatique"
            git push
          fi
