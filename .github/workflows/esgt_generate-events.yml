name: 🔄 Générer les événements ESGT

on:
  workflow_dispatch:  # Lancement manuel
  schedule:
    - cron: "0 4 * * 1"  # Tous les lundis à 4h (UTC)

jobs:
  generate-esgt:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v3

      - name: 📁 Créer le dossier static si nécessaire
        run: mkdir -p static

      - name: 🐍 Installer Python et les dépendances
        run: |
          python -m pip install --upgrade pip
          pip install ics pytz requests

      - name: ⚙️ Exécuter le script ESGT
        run: |
          echo "🧪 Lancement du script esgt_generate_events.py"
          python esgt_generate_events.py

      - name: 📤 Commit & push du JSON généré (s'il existe)
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"

          if [ -f static/esgt_events.json ]; then
            git add static/esgt_events.json

            if git diff --cached --quiet; then
              echo "✅ Aucun changement détecté à committer."
            else
              git commit -m "🔄 Mise à jour automatique des événements ESGT"
              git push
              echo "✅ Commit effectué."
            fi
          else
            echo "❌ Le fichier static/esgt_events.json n'existe pas."
            exit 1
          fi
