name: Générer événements ESGT

on:
  workflow_dispatch:  # déclenchement manuel
  schedule:
    - cron: "0 4 * * 1"  # chaque lundi à 4h du matin

jobs:
  generate-esgt-events:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v3

      - name: 🐍 Installer Python et dépendances
        run: |
          python -m pip install --upgrade pip
          pip install ics pytz requests

      - name: ⚙️ Générer le fichier JSON ESGT
        run: |
          python generate_esgt_json.py

      - name: 📤 Commit et push du fichier
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add esgt_events.json
          git commit -m "🔄 Mise à jour automatique des événements ESGT"
          git push
